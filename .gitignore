# dependencies
node_modules/

# build output (not versioned; consumer rebuilds with `pnpm install && pnpm build`)
dist/

# npm
*.tgz
*.log

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# 本机专属泄漏模式（供 scripts/check-leaks.mjs 使用）：含组织名/约定目录名等。
# 不进版本库——这样扫描器与被扫仓库都不必写出这些词。
.leak-patterns.local

